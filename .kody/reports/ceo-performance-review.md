# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

Eleventh week, picture unchanged from week 10: of 5 active duties, only ceo and tech-writer produced observable output; cto/qa/clear-empty-goals are now in their eleventh consecutive week of the same structural blockers (missing labels, missing owner, no state). Operator-driven work again dominated the window (the 26-issue 2026-06-08 batch carried into this tick, plus three new operator commits on 2026-06-09 — `feat: improve files page interactions`, `Fix Fly terminal bridge tunnel`, and the follow-up `fix: restore kody init workflow install` that accompanied the duty-enable-toggle landing).

| Staff        | Owned duties  | Delivery | Consistency | Signal  | Grade   |
| ------------ | ------------- | -------- | ----------- | ------- | ------- |
| ceo          | 2 (1 active)  | High     | High        | High    | strong  |
| coo          | 3 (0 active)  | —        | —           | —       | idle    |
| cto          | 4 (1 active)  | Unclear  | Unclear     | Unclear | unclear |
| kody         | 11 (0 active) | —        | —           | —       | idle    |
| qa           | 3 (1 active)  | Low      | Low         | Low     | weak    |
| tech-writer  | 2 (2 active)  | Med      | Med         | Med     | steady  |
| ux-designer  | 1 (0 active)  | —        | —           | —       | idle    |

- **cto — unclear (eleventh week, unchanged):** dev-ci-health is the only active duty (15m cadence, would expect ~672 runs/week). The `kody:dev-ci-red` label still does not exist in the repo (23 `kody:` labels checked this tick — same set as last week; no churn). No `.kody/duties/dev-ci-health.state.json` has ever been committed; no `.kody/reports/dev-ci-health.md` has ever been committed. The duty body still claims the dev-ci-scheduler wrapper is the precondition, but no duty owns creating the label. **Effect:** the duty cannot deliver without an operator creating the label; the structural finding is now in its 11th consecutive week.

- **qa — weak (eleventh week, unchanged):** qa-verify is active (30m cadence, ~336 runs/week expected). The `kody:cto-decisions` trust-ledger label still does not exist in the repo; the `kody:ui-verified` / `kody:ui-failed` labels **do** exist (added by the operator earlier) but a fresh sweep of the 30 most recently merged PRs shows zero stamps on either label. No `.kody/duties/qa-verify.state.json` has ever been committed; no `.kody/reports/qa-verify.md` has ever been committed. **Effect:** the verdict→merge pipeline is open-loop for the 11th week; every recent delivery PR bypasses QA; the auto-merge branch is structurally dead until `kody:cto-decisions` is created.

- **tech-writer — steady (eleventh week, mostly unchanged):** both halves moved from "no output ever" to "ran their first setup check this week" — docs-code at 2026-06-09 08:40:30Z (`.kody/reports/docs-code.md` committed: coverage heuristic passes, 15/15 candidate folders at or near full coverage; the `useMediaQuery.ts` 1-line gap is the only sub-100% folder, sub-threshold for a tracking issue), docs-readme at 2026-06-09 04:56:53Z (`.kody/reports/docs-readme.md` committed: area→doc map in sync with the live `docs/` tree). Both surface the same engine-verb blocker: `chore --issue` is not in the engine README (verbs present in the freshly-checked engine list: `run`, `resolve`, `sync`, `merge`, `revert`, `release`, `release-prepare`, `release-publish`, `release-deploy`, `preview-build`, `duty-tick`, `duty-tick-scripted`, `duty-scheduler`, `goal-scheduler`, `goal-tick`, `init`, `worker-ask`, `chat`, `serve`, `brain-serve`, `pool-serve`, `runner-serve`, `stats`, `ci`). The operator's only unblocker is either (a) add a `chore` executable, or (b) substitute `run --issue` in the recommendation template. The duties themselves remain structurally a no-op — the setup checks document the state, they do not open drift issues or recommend PRs. **Effect:** real value, but only as a snapshot generator; still not a drift detector. The setup-check pass is net-new this week and nudges the duty from "not yet running" to "running but inert" — same `steady` grade because the actionable work (recommend doc PRs) is still blocked on the same engine verb.

- **clear-empty-goals (unowned, eleventh week, drifting wider):** active every 1d, frontmatter still has no `staff:` field. Last report write 2026-06-07 09:53:00Z — now ~83h ago, ~59h overdue vs the 1d cadence (~24h wider than last week's reading; the rate of widening is now steady at ~24h/week — a clear acceleration from the prior ~2h/week trend). The report on disk still scans 1 active goal (`kody-state-split`, 5 open tasks #50–#54), closed 0; the untracked `.kody/goals/ai-company-orchestration-7-gap-plan/state.json` (active since 2026-06-07 19:02Z) is still not incorporated. **Effect:** the process gap from the prior 10 weeks is still open; likely owner is coo given their planning/audit posture, but assigning it is the operator call.

**Changes since last week:** none on grades — ceo strong→strong, coo idle→idle, cto unclear→unclear, kody idle→idle, qa weak→weak, tech-writer steady→steady, ux-designer idle→idle. The structural gaps in cto/qa and the unowned clear-empty-goals are now in their 11th consecutive week. The only net-new shape in the window was operator-driven, not duty-driven: four new operator commits on 2026-06-09 (`feat: improve files page interactions` `1f0030ce`, `Fix Fly terminal bridge tunnel` `de62042a`, `fix: restore kody init workflow install` `12086f54`, plus the follow-up `feat: add duty enabled toggle` `b01cb77b` that landed alongside the prior week's `575e6c21`), and a new doc-coverage issue #163 (with its #164 follow-up duplicate) opened 2026-06-09 — still open, still `kody:done`-stamped. The two new tech-writer setup-check reports (docs-code, docs-readme) are the only duty-driven output in the window and are both first-tick setup artifacts, not drift work.

**Structural note (cross-cutting, unchanged from the last ten weeks):** of the 5 active duties, 2 depend on labels that still do not exist in the repo (`kody:dev-ci-red` for cto/dev-ci-health, `kody:cto-decisions` for qa/qa-verify auto-merge branch). The other 3 (tech-writer two halves + clear-empty-goals) depend on either an engine verb that does not exist (`chore --issue`) or an owner who does not exist (clear-empty-goals has no `staff:` field). When a duty's own `gh label create` / `engine verb add` / `staff: <slug>` step does not run — or runs in a path that silently swallows its own error — the duty becomes structurally unable to deliver without surfacing a visible failure. The coo-owned duty-review and system-audit would catch this pattern at the design level if/when they are enabled.

**State-observability note (cross-cutting, unchanged from the last ten weeks):** of the 5 active duties, **none** of cto/dev-ci-health, qa/qa-verify, tech-writer/docs-code, or tech-writer/docs-readme has a `.kody/duties/<slug>.state.json` ever committed to the repo. The ceo-performance-review itself is also still missing state between ticks (the seed state was unchanged at start of this tick). The tech-writer setup checks *did* set `data.lastRunISO` / `data.lastCheckedMergedAt` in their first tick — but the state file path is the engine's, and none of those four duties' state files are on disk to confirm. **Effect:** every "is this duty actually running?" question is still answered by inference (label drift, PR stamps, opened issues, report files) rather than by direct evidence.

**Self-finding (ceo-performance-review, eleventh week, unchanged):** the prior state shape (`{ cursor: "seed", data: {} }`) — confirmed unchanged at start of this tick — means the closing block is not being persisted between ticks. The report file has been committed 77 times in the past 7 days (latest at 21:14:03Z for week 10, then again on the same body at subsequent wakes; week 11 is the first one where the body content materially changed), well above the `every: 7d` cadence — the engine re-fires on every wake because `lastRunISO` is never written. **Effect:** next run readout cannot show a real cadence; cycle counter never advances through the state path; deltas in this report are hand-computed from the prior report body, not from `data.lastGrades`. This is now week 11 of the same finding.