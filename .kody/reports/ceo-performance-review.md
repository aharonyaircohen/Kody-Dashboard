# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

Tenth week, picture unchanged from week 9: of 5 active duties, only ceo and tech-writer produced observable output; cto/qa/clear-empty-goals are now in their tenth consecutive week of the same structural blockers (missing labels, missing owner, no state). Operator-driven work dominated again (26 issues closed in a 2026-06-08 batch, plus 16 on 2026-06-07 — most of last week's "shipped" volume is operator mass-merge of pre-staged fixes, not duty output).

| Staff        | Owned duties  | Delivery | Consistency | Signal  | Grade   |
| ------------ | ------------- | -------- | ----------- | ------- | ------- |
| ceo          | 2 (1 active)  | High     | High        | High    | strong  |
| coo          | 3 (0 active)  | —        | —           | —       | idle    |
| cto          | 4 (1 active)  | Unclear  | Unclear     | Unclear | unclear |
| kody         | 11 (0 active) | —        | —           | —       | idle    |
| qa           | 3 (1 active)  | Low      | Low         | Low     | weak    |
| tech-writer  | 2 (2 active)  | Med      | Med         | Med     | steady  |
| ux-designer  | 1 (0 active)  | —        | —           | —       | idle    |

- **cto — unclear (tenth week, unchanged):** dev-ci-health is the only active duty (15m cadence, would expect ~672 runs/week). The `kody:dev-ci-red` label still does not exist in the repo (23 `kody:` labels checked this tick — same set as last week; no churn). No `.kody/duties/dev-ci-health.state.json` has ever been committed; no `.kody/reports/dev-ci-health.md` has ever been committed. The duty body still claims the dev-ci-scheduler wrapper is the precondition, but no duty owns creating the label. **Effect:** the duty cannot deliver without an operator creating the label; the structural finding is now in its 10th consecutive week.

- **qa — weak (tenth week, unchanged):** qa-verify is active (30m cadence, ~336 runs/week expected). The `kody:cto-decisions` trust-ledger label still does not exist in the repo; the `kody:ui-verified` / `kody:ui-failed` labels **do** exist (added by the operator earlier) but a fresh sweep of the 30 most recently merged PRs shows zero stamps on either label. No `.kody/duties/qa-verify.state.json` has ever been committed; no `.kody/reports/qa-verify.md` has ever been committed. **Effect:** the verdict→merge pipeline is open-loop for the 10th week; every recent delivery PR bypasses QA; the auto-merge branch is structurally dead until `kody:cto-decisions` is created.

- **tech-writer — steady (tenth week, unchanged):** both halves re-ran their setup checks again today — docs-code at 08:40:30Z (coverage heuristic passes, 15/15 candidate folders at or near full coverage; the `useMediaQuery.ts` 1-line gap is the only sub-100% folder), docs-readme at 04:56:53Z (area→doc map in sync with the live `docs/` tree). Both surface the same engine-verb blocker: `chore --issue` is not in the engine README (verbs present: `run`, `resolve`, `sync`, `merge`, `revert`, `release*`, `preview-build`, `duty-tick`, `duty-scheduler`, `goal-tick`, `init`, `worker-ask`, `fix`, `fix-ci`, `plan-verify`, `qa-goal`, `probe-skill`). The operator's only unblocker is either (a) add a `chore` executable, or (b) substitute `run --issue` in the recommendation template. The duty itself remains structurally a no-op — the setup checks document the state, they do not open drift issues or recommend PRs. **Effect:** real value, but only as a snapshot generator; still not a drift detector.

- **clear-empty-goals (unowned, tenth week, drifting wider):** active every 1d, frontmatter still has no `staff:` field. Last report write 2026-06-07 09:54:20Z — now ~59h ago, ~35h overdue vs the 1d cadence (~2h wider than last week reading; the rate of widening is now steady at ~2h/week). The report on disk still scans 1 active goal (`kody-state-split`, 5 open tasks #50–#54), closed 0; the untracked `.kody/goals/ai-company-orchestration-7-gap-plan/state.json` (active since 2026-06-07 19:02Z) is still not incorporated. **Effect:** the process gap from the prior 9 weeks is still open; likely owner is coo given their planning/audit posture, but assigning it is the operator call.

**Changes since last week:** none on grades — ceo strong→strong, coo idle→idle, cto unclear→unclear, kody idle→idle, qa weak→weak, tech-writer steady→steady, ux-designer idle→idle. The structural gaps in cto/qa and the unowned clear-empty-goals are now in their 10th consecutive week. The only new shape in the window was operator-driven, not duty-driven: 26 issues closed on 2026-06-08 (vs 16 on 2026-06-07, +10 — same batch pattern as prior weeks, P1s plus feature/bug work), three new `kody:docs-coverage` issues #157/#159/#161 opened 2026-06-08–09 (still open), and a UI feature landed 2026-06-09: `feat: add duty enable toggle` (commits `575e6c21` + `b01cb77b`) — operator-built, not duty-built, and gives operators a per-duty enable switch but does not by itself unblock the structurally-blocked duties above.

**Structural note (cross-cutting, unchanged from the last nine weeks):** of the 5 active duties, 2 depend on labels that still do not exist in the repo (`kody:dev-ci-red` for cto/dev-ci-health, `kody:cto-decisions` for qa/qa-verify auto-merge branch). The other 3 (tech-writer two halves + clear-empty-goals) depend on either an engine verb that does not exist (`chore --issue`) or an owner who does not exist (clear-empty-goals has no `staff:` field). When a duty own `gh label create` / `engine verb add` / `staff: <slug>` step does not run — or runs in a path that silently swallows its own error — the duty becomes structurally unable to deliver without surfacing a visible failure. The coo-owned duty-review and system-audit would catch this pattern at the design level if/when they are enabled.

**State-observability note (cross-cutting, unchanged from the last nine weeks):** of the 5 active duties, **none** of cto/dev-ci-health, qa/qa-verify, tech-writer/docs-code, or tech-writer/docs-readme has a `.kody/duties/<slug>.state.json` ever committed to the repo. The ceo-performance-review itself is also still missing state between ticks (the seed state was unchanged at start of this tick). **Effect:** every active staff is this duty actually running? question is answered by inference (label drift, PR stamps, opened issues, report files) rather than by direct evidence.

**Self-finding (ceo-performance-review, tenth week, unchanged):** the prior state shape (`{ cursor: "seed", data: {} }`) — confirmed unchanged at start of this tick — means the closing block is not being persisted between ticks. The report file has been committed 10+ times in the past 7 days (latest at 19:16:07Z for week 10; week 9 pattern was 12/day), well above the `every: 7d` cadence — the engine re-fires on every wake because `lastRunISO` is never written. **Effect:** next run readout cannot show a real cadence; cycle counter never advances through the state path; deltas in this report are hand-computed from the prior report body, not from `data.lastGrades`. This is now week 10 of the same finding.
