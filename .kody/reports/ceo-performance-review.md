# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

Fourteenth week, picture unchanged from week 13: of 5 active duties, only ceo and tech-writer produced observable output; cto/qa/clear-empty-goals are now in their fourteenth consecutive week of the same structural blockers (missing labels, zero stamps, no committed state). Operator-driven work continued in the window — four new operator commits on 2026-06-10 (`Add docs management and preview updates`, `Add memory manager and docs navigation`, `feat(context): add ideas`, plus an infra `chore(dashboard): update preview environments` triplet) — all dashboard UI surface work, none addressing the duty-side blockers. The two untracked goal state files in the worktree (`.kody/goals/{kody-state-split,ai-company-orchestration-7-gap-plan}/state.json`) confirm clear-empty-goals *did* run in the window; its on-disk state simply did not survive to a commit.

| Staff        | Owned duties  | Delivery | Consistency | Signal | Grade   |
| ------------ | ------------- | -------- | ----------- | ------ | ------- |
| ceo          | 2 (1 active)  | High     | High        | High   | strong  |
| coo          | 3 (0 active)  | —        | —           | —      | idle    |
| cto          | 4 (1 active)  | Unclear  | Unclear     | Unclear| unclear |
| kody         | 11 (0 active) | —        | —           | —      | idle    |
| qa           | 3 (1 active)  | Low      | Low         | Low    | weak    |
| tech-writer  | 2 (2 active)  | Med      | Med         | Med    | steady  |
| ux-designer  | 1 (0 active)  | —        | —           | —      | idle    |
| *(unowned)*  | 1 (1 active)  | Med      | Med         | Med    | unclear |

- **cto — unclear (fourteenth week, unchanged):** dev-ci-health is the only active duty (15m cadence, would expect ~672 runs/week). The `kody:dev-ci-red` label still does not exist in the repo (24 `kody:*` labels checked this tick, same count as week 12). No `.kody/duties/dev-ci-health.state.json` has ever been committed; no `.kody/reports/dev-ci-health.md` has ever been written. The duty body still claims the dev-ci-scheduler wrapper is the precondition, but no duty owns creating the label. **Effect:** the duty cannot deliver without an operator creating the label; the structural finding is now in its 14th consecutive week.
- **qa — weak (fourteenth week, unchanged):** qa-verify is active (30m cadence, ~336 runs/week expected). The `kody:cto-decisions` trust-ledger label still does not exist; the `kody:ui-verified` / `kody:ui-failed` labels *do* exist (added by the operator earlier) but a fresh sweep of every PR/issue labeled with either returns **zero** stamps — same as week 13. No `.kody/duties/qa-verify.state.json` has ever been committed; no `.kody/reports/qa-verify.md` has ever been written. **Effect:** the verdict-merge pipeline is open-loop for the 14th week; every recent deliverer PR bypasses QA; the auto-merge branch is structurally dead until `kody:cto-decisions` is created.
- **tech-writer — steady (fourteenth week, mostly unchanged):** both halves remain in their post-setup posture from the prior week — docs-code at 2026-06-09 08:40:30Z (`.kody/reports/docs-code.md` committed: coverage heuristic passes, 15/15 candidate folders at or near full coverage; the `useMediaQuery.ts` 1-line gap is the only sub-100% folder, sub-threshold for a tracking issue), docs-readme at 2026-06-10 00:24:14Z (`.kody/reports/docs-readme.md` recheck: the engine verb blocker is unchanged). Both reports still surface the same engine-verb blocker: built-in executables are `duty-scheduler`, `duty-tick`, `duty-tick-scripted`, `fix`, `fix-ci`, `goal-scheduler`, `goal-tick`, `init`, `job-live-verify`, `merge`, `plan-verify`, `preview-build`, `probe-skill`, `qa-goal`, `release*`, `resolve`, `revert`, `run`, `sync`, `worker-ask`. No `chore`. The set has churned since last week (`chat`, `serve`, `brain-serve`, `pool-serve`, `runner-serve`, `stats`, `ci` were last week's listed verbs; `fix`, `fix-ci`, `job-live-verify`, `plan-verify`, `probe-skill`, `qa-goal`, `task-job-*` are new) — but `chore` is still absent. The operator's only unblocker is either (a) add a `chore` executable, or (b) substitute `run --issue` in the recommendation template. The duties themselves remain structurally a no-op — the setup checks document the state, they do not open drift issues or recommend PRs. **Effect:** same `steady` grade because the actionable work (recommend doc PRs) is still blocked on the same engine verb.
- **clear-empty-goals (unowned, fourteenth week, drifting wider):** active every 1d, frontend still has no `staff:` field. The on-disk report (`.kody/reports/clear-empty-goals.md`) was last generated 2026-06-07 09:53:00Z — the body content is now three days old. The untracked `.kody/goals/kody-state-split/state.json` (active since 2026-06-07 19:02Z) is still visible in this worktree's git status as `A` (staged/added) — operator has been preparing to commit it for six days now and has not. The untracked `.kody/runs/` directory has appeared in this worktree's git status this week (untracked, not staged). The `goal:ai-company-orchestration-7-gap-plan` label has 5 open issues (#92, #93, #94, #96, #97) and 3 closed (#90, #91, #95); none of the closed ones closed in this window. **Effect:** the process gap from the prior 12 weeks is still open; the on-disk report is now stale (3d) and the unowned duty has not regenerated it. Likely owner is still coo given their planning/audit posture, but assigning it is the operator call.

**Changes since last week:** none on grades — ceo strong→strong, coo idle→idle, cto unclear→unclear, kody idle→idle, qa weak→weak, tech-writer steady→steady, ux-designer idle→idle. The structural gaps in cto/qa and the unowned clear-empty-goals are now in their 14th consecutive week. The only net-new shape in the window was operator-driven, not duty-driven: one operator commit on 2026-06-10 (`chore(ceo-performance-review): refresh report` — this report's predecessor at `2026-06-10T00:15:24Z`), one on 2026-06-10 (`Refine duty control buttons` `04123bcc`, possibly the trigger of the cadence over-firing on the ceo duty), and one on 2026-06-10 (`Store reports on kody state branch` `2757f051` — suggests the reports surface may be migrating off `main`). The two tech-writer setup-check / recheck reports from the prior 13 weeks remain the only duty-driven output in the window and are still first-tick setup artifacts, not drift work.

**Structural note (cross-cutting, unchanged from the last twelve weeks):** of the 5 active duties, 2 depend on labels that still do not exist in the repo (`kody:dev-ci-red` for cto/dev-ci-health, `kody:cto-decisions` for qa/qa-verify auto-merge branch). The other 3 (tech-writer two halves + clear-empty-goals) depend on either an engine verb that does not exist (`chore --issue`) or an owner who does not exist (clear-empty-goals has no `staff:` field). When a duty's own `gh label create` / `engine verb add` / `staff: <slug>` step does not run — or runs in a path that silently swallows its own error — the duty becomes structurally unable to deliver without surfacing a visible failure. The coo-owned duty-review and system-audit would catch this pattern at the design level if/when they are enabled.

**State-observability note (cross-cutting, unchanged from the last twelve weeks):** of the 5 active duties, **none** of cto/dev-ci-health, qa/qa-verify, tech-writer/docs-code, or tech-writer/docs-readme has a `.kody/duties/<slug>.state.json` ever committed to the repo. The ceo-performance-review itself is also still missing state between ticks (the seed state was unchanged at start of this tick). The tech-writer setup checks *did* set `data.lastRunISO` / `data.lastCheckedMergedAt` in their first tick — but the state file path is the engine's, and none of those four duties' state files are on disk to confirm. **Effect:** every "is this duty actually running?" question is still answered by inference (label drift, PR stamps, opened issues, report files) rather than by direct evidence.

**Cadence note (ceo-performance-review, fourteenth week):** the report file has been committed 17 times in the past 9 days (latest at `2026-06-10T00:15:24Z` prior to this one, now this one), well above the `every: 7d` cadence. Two possible triggers for the over-firing, both consistent with the current evidence: (a) the engine is not respecting the `every: 7d` guard because `lastRunISO` is never written (the seed state is unchanged at start of this tick — same self-finding as the prior 12 weeks), or (b) the dashboard now exposes a "Run now" button (consistent with the new `Refine duty control buttons` operator commit `04123bcc`) and an operator is clicking it. The honest answer: cannot tell from this duty's data alone. The state file path remains the load-bearing artifact; until it is on disk between ticks, the cadence readout is decorative. **Effect:** next run readout still cannot show a real cadence; cycle counter still never advances through the state path; deltas in this report are hand-computed from the prior report body, not from `data.lastGrades`. This is now week 14 of the same finding.

**Reports-storage note (new this tick):** the operator commit `Store reports on kody state branch` `2757f051` suggests the reports surface is being moved off `main`. If reports are now read from a dedicated branch, the next tick must re-pin the report read path before grading. The four reports on `main` (`.kody/reports/{ceo-performance-review,clear-empty-goals,docs-code,docs-readme}.md`) remain the canonical surface for this tick — the prior report's grading was sourced from them, and the same files are what `gh api /contents/.kody/reports/...` returns against `main` this tick. **Effect:** the new branch is an unknown; cannot confirm a separate `kody-state` branch yet via `gh`. Future ticks will need to verify.
