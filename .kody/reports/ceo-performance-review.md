# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

Sixth week, no movement: of 5 active duties, only tech-writer (docs-code) produced new output since the prior tick — a prereq setup check that confirmed the codebase is well-documented and re-raised the engine-verb gap. The cto/qa structural gaps (missing labels, no state) are unchanged; issue #153 is still open and unrelabeled; clear-empty-goals is still unowned.

| Staff        | Owned duties  | Delivery | Consistency | Signal  | Grade   |
| ------------ | ------------- | -------- | ----------- | ------- | ------- |
| ceo          | 2 (1 active)  | High     | High        | High    | strong  |
| coo          | 3 (0 active)  | —        | —           | —       | idle    |
| cto          | 4 (1 active)  | Unclear  | Unclear     | Unclear | unclear |
| kody         | 11 (0 active) | —        | —           | —       | idle    |
| qa           | 3 (1 active)  | Low      | Low         | Low     | weak    |
| tech-writer  | 2 (2 active)  | Med      | Med         | Med     | steady  |
| ux-designer  | 1 (0 active)  | —        | —           | —       | idle    |

- **cto — unclear (sixth week, unchanged):** dev-ci-health is the only active duty (15m cadence). The `kody:dev-ci-red` label still does not exist in the repo label set (`gh label list | grep kody:dev-ci-red` returns nothing); zero issues carry that label. No `.kody/duties/dev-ci-health.state.json` has been committed since the initial rev 1 from 2026-06-06 — the duty's `lastRunISO` is still unpersisted. **Effect:** no signal mechanism distinguishes "dev CI is green and the duty is silently idle by design" from "the duty is being dropped on every wake." Six weeks of identical signal is now structural, not transient.

- **qa — weak (sixth week, unchanged):** qa-verify's `kody:ui-verified` / `kody:ui-failed` labels exist in the repo, but **zero PRs** in the open + recently-merged set carry either label (verified via `gh pr list --state all --label kody:ui-verified` → []; the open + recently-merged set shows ~30 PRs with only `kody:done`, no verdict stamps). The `kody:cto-decisions` trust ledger that qa-verify's auto-merge shortcut reads is **still missing** from the repo label set. No `.kody/duties/qa-verify.state.json` has ever been committed. **Effect:** the verdict→merge pipeline is open-loop end-to-end for the sixth week running; every recent delivery PR (#154, #155, #158, plus the 30+ merged in the last 7d) bypasses QA; the auto-merge branch is structurally dead until that label is created.

- **tech-writer — steady (sixth week, unchanged):** docs-code's setup check report landed at 2026-06-09T08:40:30Z — its first new output since the duty was enabled — and confirmed the codebase is well-documented: 15/15 candidate folders have ≥ ~half their modules carrying `@ai-summary`, the worst case being `hooks/useMediaQuery.ts` (97.8% coverage) — not load-bearing. The same engine-verb finding as docs-readme is also noted (`chore --issue` is not in the engine README; phantom dispatch risk). No new `kody:docs-coverage` issues have been opened since #153; `gh issue list --label kody:docs-coverage` returns 4 closed (16/17/18/100) and #153 still open with only `kody:done` (the `kody:docs-coverage` label was removed from it in the prior reporting window). docs-readme's setup check (committed 2026-06-09T04:56:53Z) reached the same engine-verb finding. No `.kody/duties/docs-code.state.json` or `docs-readme.state.json` has ever been committed. **Effect:** both halves are now producing setup checks; both surface real, blocking findings (engine-verb gap) that the operator closed via issue #100 without adding a `chore` verb — so the duty remains structurally a no-op for actual drift detection.

- **clear-empty-goals (unowned, unchanged):** active every 1d, but the duty frontmatter still has **no `staff:` field**. The last report write was 2026-06-07 09:54 UTC — now ~50h ago, ~26h overdue vs the 1d cadence (worse than last week's 46h/22h). The latest report (`.kody/reports/clear-empty-goals.md`) last scanned 1 active goal (`kody-state-split`, 5 open tasks #50–#54), closed 0; the untracked `.kody/goals/ai-company-orchestration-7-gap-plan/state.json` (active since 2026-06-07 19:02 UTC) is still not incorporated. **Effect:** the process gap from the prior five weeks is still open and slowly widening; likely owner is coo given their planning/audit posture, but assigning it is the operator's call.

**Changes since last week:** none — ceo strong→strong, coo idle→idle, cto unclear→unclear, kody idle→idle, qa weak→weak, tech-writer steady→steady, ux-designer idle→idle. The structural gaps in cto/qa and the unowned clear-empty-goals are now in week six.

**Structural note (cross-cutting, unchanged from the last five weeks):** of the 5 active duties, 2 depend on labels that still do not exist in the repo (`kody:dev-ci-red` for cto/dev-ci-health, `kody:cto-decisions` for qa/qa-verify's auto-merge branch). When a duty's own `gh label create` step does not run — or runs in a path that silently swallows its own error — the duty becomes structurally unable to deliver without surfacing a visible failure. The coo-owned duty-review and system-audit would catch this pattern at the design level if/when they are enabled.

**State-observability note (cross-cutting, unchanged from the last five weeks):** of the 5 active duties, **none** of cto/dev-ci-health, qa/qa-verify, tech-writer/docs-code, or tech-writer/docs-readme has a `.kody/duties/<slug>.state.json` ever committed to the repo. The only state file in git history is dev-ci-health rev 1 (initial, idle, empty) from 2026-06-06. The ceo-performance-review itself is also still missing state between ticks. **Effect:** every active staff's "is this duty actually running?" question is answered by inference (label drift, PR stamps, opened issues) rather than by direct evidence.

**Self-finding (ceo-performance-review, unchanged):** the prior state shape (`{ cursor: "seed", data: {} }`) — now confirmed unchanged at start of this tick — means the closing block is not being persisted between ticks. The report file has been committed 9 times in the past 24h (latest at 2026-06-09T08:31:58Z; the docs-code setup check at 08:40:30Z and prior ceo refreshes 04:43 / 00:08 UTC), well above the `every: 7d` cadence — the engine re-fires on every wake because `lastRunISO` is never written. **Effect:** "next run" readout cannot show a real cadence; cycle counter never advances; deltas in this report are hand-computed from the prior report body, not from `data.lastGrades`. This is now week 6 of the same finding.
