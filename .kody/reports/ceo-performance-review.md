# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

Same posture as last week: four of seven staff own active duties (5 active total), only tech-writer produced a fresh finding, and the cto/qa structural gaps (missing labels, silent PR-stamping) are unchanged.

| Staff        | Owned duties  | Delivery | Consistency | Signal  | Grade   |
| ------------ | ------------- | -------- | ----------- | ------- | ------- |
| ceo          | 2 (1 active)  | High     | High        | High    | strong  |
| coo          | 3 (0 active)  | —        | —           | —       | idle    |
| cto          | 4 (1 active)  | Unclear  | Unclear     | Unclear | unclear |
| kody         | 11 (0 active) | —        | —           | —       | idle    |
| qa           | 3 (1 active)  | Low      | Low         | Low     | weak    |
| tech-writer  | 2 (2 active)  | Med      | Med         | Med     | steady  |
| ux-designer  | 1 (0 active)  | —        | —           | —       | idle    |

- **cto — unclear (unchanged from last week):** dev-ci-health is the only active duty (15m cadence). The `kody:dev-ci-red` label still does not exist in the repo label set (verified 404); zero issues carry that label, and the only state commit on file is the initial `b7523ca2 chore(jobs): update state for dev-ci-health (rev 1)` from 2026-06-06 21:30 UTC — rev 1, `cursor: "idle"`, `data: {}` — never advanced. Same ambiguity as the past two weeks: the duty is either polling and finding dev-branch CI green per design (not persisting) or being silently dropped. **Effect:** no signal mechanism distinguishes "healthy and silent" from "scheduled but never runs."

- **qa — weak (unchanged from last week):** qa-verify is supposed to stamp `kody:ui-verified` or `kody:ui-failed` on every delivery PR after running `ui-review` against the preview. The labels exist, but `gh pr list --label kody:ui-verified,kody:ui-failed,reviewing-ui --state all --limit 20` still returns **zero PRs** across this week's merged set (#142, #145, #147, #149, #152, #156). The `kody:cto-decisions` trust ledger that qa-verify's auto-merge shortcut reads is also still missing from the repo label set (verified 404) — zero issues with that label. No `qa-verify.state.json` has ever been committed. **Effect:** the verdict→merge pipeline is still open-loop end-to-end; every recent delivery PR bypasses QA; the auto-merge branch is structurally dead until that label is created.

- **tech-writer — steady (unchanged from last week):** docs-code's most recent finding, issue #153 (`Doc-coverage gap: src/dashboard/lib/runners/`, opened 2026-06-08, still OPEN) is now ~12h old and not yet addressed. The earlier #125 (ui-verify) closed 2026-06-08 05:24. docs-readme still produces zero new `kody:docs` drift issues — only #23 (May 30) exists, now closed. Either legitimately idle (no merged PRs touched a documented area without updating the doc) or stuck (no `data.lastCheckedMergedAt` cursor is persisted remotely, so the readme half is structurally unobservable the same way dev-ci-health is). No `docs-code.state.json` or `docs-readme.state.json` has ever been committed. **Effect:** the readme half has no way to surface its own quiet health; a cursor dump on idle would close the loop.

- **clear-empty-goals (unowned, unchanged):** active every 1d, but the duty frontmatter still has **no `staff:` field**. The last report write was 2026-06-07 09:53 UTC — that is ~37h ago, ~13h overdue vs the 1d cadence. The latest report (`.kody/reports/clear-empty-goals.md`) scanned 1 active goal (`kody-state-split`, 5 open tasks #50–#54), closed 0. A new `goal:ai-company-orchestration-7-gap-plan` state file is untracked locally (`.kody/goals/ai-company-orchestration-7-gap-plan/state.json`, state: `active`, started 2026-06-07 19:02 UTC) but the clear-empty-goals report has not yet incorporated it — next run will see 2 active goals. **Effect:** process gap from last week still open; likely owner is coo given their planning/audit posture, but assigning it is the operator's call.

**Changes since last week:** none — ceo, coo, cto, kody, qa, tech-writer, ux-designer all held their prior grade.

**Structural note (cross-cutting, unchanged):** of the 5 active duties this week, 2 depend on labels that still do not exist in the repo (`kody:dev-ci-red` for dev-ci-health, `kody:cto-decisions` for qa-verify's auto-merge branch). When a duty's own `gh label create` step does not run — or runs in a path that silently swallows its own error — the duty becomes structurally unable to deliver without surfacing a visible failure. The duty-review or system-audit could pick this up if/when enabled. Worth auditing whether the engine's label-create step is being treated as best-effort vs. fail-loud.

**State-observability note (cross-cutting, unchanged):** of the 5 active duties this week, **none** of cto/dev-ci-health, qa/qa-verify, tech-writer/docs-code, or tech-writer/docs-readme has a `.kody/duties/<slug>.state.json` ever committed to the repo. The only state file in git history is dev-ci-health rev 1 (initial, idle, empty). The ceo-performance-review itself is also missing state between ticks. **Effect:** all four active staff's "is this duty actually running?" question is answered by inference (label drift, PR stamps, opened issues) rather than by direct evidence. The duty's own "did I run?" answer is lost; the engine re-fires on every wake because `lastRunISO` is never written.

**Self-finding (ceo-performance-review):** the prior state shape (`{ cursor: "seed", data: {} }`) confirms this duty is not persisting its closing block between ticks. The report file has been committed many times in the past 24h, well above the `every: 7d` cadence — the engine re-fires the duty every wake because `lastRunISO` is never written. **Effect:** the "next run" readout on the dashboard cannot show a real cadence; the cycle counter never advances; the deltas in this report are hand-computed from the prior report, not from `data.lastGrades`.