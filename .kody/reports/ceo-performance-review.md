# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

Eighth week, picture unchanged from week 7: 4 of 5 active duties still blocked on missing preconditions (labels, engine verbs, owners); tech-writer delivered setup checks on both halves; ceo's own loop ran 12× in 7 days because state still doesn't persist.

| Staff        | Owned duties  | Delivery | Consistency | Signal  | Grade   |
| ------------ | ------------- | -------- | ----------- | ------- | ------- |
| ceo          | 2 (1 active)  | High     | High        | High    | strong  |
| coo          | 3 (0 active)  | —        | —           | —       | idle    |
| cto          | 4 (1 active)  | Unclear  | Unclear     | Unclear | unclear |
| kody         | 10 (0 active) | —        | —           | —       | idle    |
| qa           | 3 (1 active)  | Low      | Low         | Low     | weak    |
| tech-writer  | 2 (2 active)  | Med      | Med         | Med     | steady  |
| ux-designer  | 1 (0 active)  | —        | —           | —       | idle    |

- **cto — unclear (eighth week, unchanged):** dev-ci-health is the only active duty (15m cadence, would expect ~672 runs/week). The `kody:dev-ci-red` label still does not exist in the repo (43 labels checked, no `kody:dev-ci-red` and no `kody:cto-decisions`). No `.kody/duties/dev-ci-health.state.json` has ever been committed; no `.kody/reports/dev-ci-health.md` has ever been committed. The duty body still claims the dev-ci-scheduler wrapper is the precondition, but no duty owns creating the label. **Effect:** the duty cannot deliver without an operator creating the label; the structural finding is now in its 8th consecutive week.

- **qa — weak (eighth week, unchanged):** qa-verify is active (30m cadence, ~336 runs/week expected). The `kody:cto-decisions` trust-ledger label still does not exist in the repo; the `kody:ui-verified` / `kody:ui-failed` labels **do** exist (added by the operator earlier) but zero of the 30 most recently merged PRs (#135–#156) carry either stamp. No `.kody/duties/qa-verify.state.json` has ever been committed; no `.kody/reports/qa-verify.md` has ever been committed. **Effect:** the verdict→merge pipeline is open-loop for the 8th week; every recent delivery PR bypasses QA; the auto-merge branch is structurally dead until `kody:cto-decisions` is created.

- **tech-writer — steady (eighth week, unchanged):** both halves ran their setup checks today — docs-code at 08:40:30Z (coverage heuristic passes, 15/15 candidate folders at or near full coverage), docs-readme at 04:56:53Z (area→doc map in sync with the live `docs/` tree). Both surface the same engine-verb blocker: `chore --issue` is not in the engine README. The operator closed issue #100 (the duty's own report) on 2026-06-07 19:21Z and the `kody:docs-coverage` issues #16/#17/#18/#99/#120/#125 closed en masse the same day, but the fix path was operator-driven `@kody run`, not a duty dispatch. The duty itself remains structurally a no-op — the setup checks document the state, they don't open drift issues or recommend PRs. **Effect:** real value, but only as a snapshot generator; still not a drift detector.

- **clear-empty-goals (unowned, eighth week, still drifting):** active every 1d, frontmatter still has no `staff:` field. Last report write 2026-06-07 09:54Z — now ~52h ago, ~28h overdue vs the 1d cadence (flat with last week, still widening slowly). The latest report scanned 1 active goal (`kody-state-split`, 5 open tasks #50–#54), closed 0; the untracked `.kody/goals/ai-company-orchestration-7-gap-plan/state.json` (active since 2026-06-07 19:02Z) is still not incorporated. **Effect:** the process gap from the prior 7 weeks is still open; likely owner is coo given their planning/audit posture, but assigning it is the operator's call.

**Changes since last week:** none on grades — ceo strong→strong, coo idle→idle, cto unclear→unclear, kody idle→idle, qa weak→weak, tech-writer steady→steady, ux-designer idle→idle. The structural gaps in cto/qa and the unowned clear-empty-goals are now in their 8th consecutive week. The only new shape in the window was tech-writer's two setup checks landing on the same day, both surfacing the same engine-verb finding; operator-driven work continued at the same pace (issues #157→#158, #159→#160, #100 closed, plus manual `runner-router.ts` folder header) — none of it duty-driven.

**Structural note (cross-cutting, unchanged from the last seven weeks):** of the 5 active duties, 2 depend on labels that still do not exist in the repo (`kody:dev-ci-red` for cto/dev-ci-health, `kody:cto-decisions` for qa/qa-verify's auto-merge branch). The other 3 (tech-writer's two halves + clear-empty-goals) depend on either an engine verb that doesn't exist (`chore --issue`) or an owner who doesn't exist (clear-empty-goals has no `staff:` field). When a duty's own `gh label create` / `engine verb add` / `staff: <slug>` step does not run — or runs in a path that silently swallows its own error — the duty becomes structurally unable to deliver without surfacing a visible failure. The coo-owned duty-review and system-audit would catch this pattern at the design level if/when they are enabled.

**State-observability note (cross-cutting, unchanged from the last seven weeks):** of the 5 active duties, **none** of cto/dev-ci-health, qa/qa-verify, tech-writer/docs-code, or tech-writer/docs-readme has a `.kody/duties/<slug>.state.json` ever committed to the repo. The ceo-performance-review itself is also still missing state between ticks (the seed state was unchanged at start of this tick). **Effect:** every active staff's "is this duty actually running?" question is answered by inference (label drift, PR stamps, opened issues, report files) rather than by direct evidence.

**Self-finding (ceo-performance-review, eighth week, unchanged):** the prior state shape (`{ cursor: "seed", data: {} }`) — confirmed unchanged at start of this tick — means the closing block is not being persisted between ticks. The report file has been committed 12 times in the past 7 days (latest at 14:13:05Z; docs-code setup check at 08:40:30Z and docs-readme setup check at 04:56:53Z bookend the window), well above the `every: 7d` cadence — the engine re-fires on every wake because `lastRunISO` is never written. **Effect:** "next run" readout cannot show a real cadence; cycle counter never advances through the state path; deltas in this report are hand-computed from the prior report body, not from `data.lastGrades`. This is now week 8 of the same finding.
