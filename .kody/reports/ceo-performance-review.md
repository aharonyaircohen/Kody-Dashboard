# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

**Week 17.** Same shape as week 16: 5 active duties all run on schedule (~7×/day each, all `outcomeKind: ok` per the kody-state activity log), 3 of them — `dev-ci-health`, `qa-verify`, `docs-readme` — complete without producing their gate artifacts, and the `clear-empty-goals` duty still has zero entries in the activity log. The structural finding from week 16 (`reason: null` on every event) is unchanged. The new finding this week: the structured state machine is still not persisting between ticks — the input to this tick was `cursor: "seed", data: {}` for the second consecutive week, so the engine has had no `data.lastRunISO` on disk to enforce `every: 7d` against. Ceo cadence remains structurally unenforced.

| Staff        | Owned duties  | Delivery | Consistency | Signal | Grade   |
| ------------ | ------------- | -------- | ----------- | ------ | ------- |
| ceo          | 2 (1 active)  | High     | Med         | High   | steady  |
| coo          | 3 (0 active)  | —        | —           | —      | idle    |
| cto          | 4 (1 active)  | Med      | High        | Low    | unclear |
| kody         | 11 (0 active) | —        | —           | —      | idle    |
| qa           | 3 (1 active)  | Med      | High        | Low    | weak    |
| tech-writer  | 2 (2 active)  | High     | High        | Med    | steady  |
| ux-designer  | 1 (0 active)  | —        | —           | —      | idle    |
| *(unowned)*  | 1 (1 active)  | Low      | Low         | Low    | weak    |

- **ceo — steady (week 17, downgraded from strong):** same over-fire, 49 runs in 7 days (6, 10, 1, 11, 8, 9, 4) when the promise is 1. The over-fire is the same as week 16, but the downgrade reflects the second consecutive week where `data.lastRunISO` was not on disk at the start of the tick — meaning the engine has had two opportunities to start persisting the state and has not, so the cadence contract is structurally unenforced from the engine side, not just a duty-side concern. **Effect:** the dashboard "next run" readout remains decorative for the foreseeable future.
- **cto — unclear (17th week, unchanged):** `dev-ci-health` runs ~7×/day and completes `ok`; still no `kody:dev-ci-red`/`kody:dev-ci-green` label on any issue (verified — 0 hits), still no `.kody/reports/dev-ci-health.md` on disk, and the only structured state file anywhere is still `.kody/duties/dev-ci-health.state.json` (cursor `idle`, data `{}`) — last touched on 2026-06-06T21:30:25Z, **4 days and 49 runs ago, still rev 1**. The state file was created once and never updated, so the duty is failing to write its own `data.lastRunISO` either. The activity log records `outcome` but not the reason a tick completed without acting. **Effect:** if `dev` is green, the duty is doing its job and we cannot see it; if it is broken, the operator has not been notified for 17 weeks.
- **qa — weak (17th week, unchanged):** `qa-verify` runs ~7×/day and completes `ok`; still **0** PRs across the entire repo carry `kody:ui-verified` or `kody:ui-failed` (verified — 0 hits on both label queries), and the `kody:cto-decisions` trust-ledger label still does not exist. **9** open PRs currently carry `kody:done` and are waiting on a verdict — up from 7 last week (#144, #155, #158, #160, #162, #164, #154, #109, #71, all `kody:done`, all unverified). **Effect:** verdict→merge pipeline remains open-loop; the docs-coverage PRs that docs-code recommends cannot complete the qa-verify gate, so they sit in the queue indefinitely.
- **clear-empty-goals (unowned, 17th week, unchanged):** still 0 entries in the activity log across 7 sampled days (2026-06-03 → 2026-06-10); the on-disk report at `.kody/reports/clear-empty-goals.md` is still the 2026-06-07 09:53:00Z finding (now 3+ days stale); the untracked `.kody/goals/{kody-state-split,ai-company-orchestration-7-gap-plan}/state.json` files are still staged/added in the worktree (same as weeks 14, 15, and 16). **Effect:** the manifest is being mutated faster than it can be scanned; the unowned duty has now been effectively stopped for three consecutive weeks.

**Changes since last week:** ceo strong→steady. coo idle→idle; cto unclear→unclear; kody idle→idle; qa weak→weak; tech-writer steady→steady; ux-designer idle→idle; *(unowned)* weak→weak. The ceo downgrade is honest: the over-fire was the same in week 16, but the second consecutive tick with `cursor: "seed"` at input makes it clear the engine is not going to enforce cadence by itself, so the duty is structurally over-firing.

**Activity log (week 17, 244 events across 7 days):** all 5 active duties ran on schedule (~7×/day each), all `outcome: completed`, all `outcomeKind: ok`, all `reason: null`. Per-day counts: 2026-06-03 = 30, 2026-06-04 = 50, 2026-06-05 = 5, 2026-06-07 = 54, 2026-06-08 = 40, 2026-06-09 = 45, 2026-06-10 = 20. (2026-06-06 missing from the log entirely — engine gap, not duty gap; the schedule on that day may not have run.) `docs-readme` is the only duty that missed a single slot (48 vs 49 for the others).

**State machine (week 17, second tick):** the input to this tick was `{"version":1,"rev":0,"cursor":"seed","data":{}}` — the same empty seed state as week 16. The engine has not persisted the `ceo-performance-review.state.json` between the two ticks, so `data.cycle: 1` (emitted last week) and `data.cycle: 2` (this week) live only in this report body, not on disk. Cadence enforcement therefore cannot work until the engine starts writing the state file. Of the 5 active duties, only `dev-ci-health` has a state file on the kody-state branch — and it has not been touched in 4 days (49 runs since the last write).

**Operator-driven work in the window:** no new operator commits to `main` since the week-16 report's cut-off. All activity in the window is duty-driven: ceo over-fire (49 writes), 3 new docs-coverage PRs (#160, #162, #164), 1 docs-drift PR (#109, the #24 issue), and 2 docs-code/docs-readme report refreshes (still "chore verb missing" — same finding as last 2 weeks).
