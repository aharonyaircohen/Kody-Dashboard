# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

**Week 20.** Same shape as week 19: 5 active duties all ran on schedule (~7.7×/day each, all `outcomeKind: ok`, all `reason: null`), the gate artifacts (`kody:dev-ci-red/green`, `kody:ui-verified/failed`, `kody:cto-decisions`) are still 0 hits in the repo, the unowned `clear-empty-goals` duty still has zero entries in the activity log, and the `docs-readme`/`docs-code` setup-check reports still cite the missing engine `chore` verb as the reason both duties are no-ops. The structural finding from week 19 — the engine has now failed to persist `ceo-performance-review.state.json` for the **fifth** consecutive tick — is unchanged; the operator's `duty enable toggle` series (`feat: add duty enabled toggle`, `feat: add duty enable toggle`, `Refine duty control buttons`, `Store reports on kody state branch`) is now in `main` and, if the scheduler is wired to honor it, is the structural remediation for the over-fire pattern (the toggles are UI-visible this week, but 2026-06-10 still logged 35 events across 5 duties — the scheduler has not yet started enforcing them).

| Staff        | Owned duties  | Delivery | Consistency | Signal | Grade   |
| ------------ | ------------- | -------- | ----------- | ------ | ------- |
| ceo          | 2 (1 active)  | High     | Med         | High   | steady  |
| coo          | 3 (0 active)  | —        | —           | —      | idle    |
| cto          | 4 (1 active)  | Med      | High        | Low    | unclear |
| kody         | 11 (0 active) | —        | —           | —      | idle    |
| qa           | 3 (1 active)  | Med      | High        | Low    | weak    |
| tech-writer  | 2 (2 active)  | High     | High        | Med    | steady  |
| ux-designer  | 1 (0 active)  | —        | —           | —      | idle    |
| *(unowned)*  | 1 (1 active)  | Low      | Low         | Low    | weak    |

- **cto — unclear (20th week, unchanged):** `dev-ci-health` ran ~7.7×/day this week (46 events 2026-06-04 → 2026-06-10, all `ok`, all `reason: null`); still **0** issues carry `kody:dev-ci-red` or `kody:dev-ci-green` (verified — 0 hits on both label queries); still no `.kody/reports/dev-ci-health.md`; the only structured state file anywhere is still `.kody/duties/dev-ci-health.state.json` on `kody-state` (cursor `idle`, data `{}`, rev 1) — last touched 2026-06-06T21:30:25Z, **4 days ago and ~60 runs since the last write**. The activity log records `outcome` but not the reason a tick completed without acting. **Effect:** if `dev` is green, the duty is doing its job and we cannot see it; if it is broken, the operator has not been notified for 20 weeks. Same as weeks 16, 17, 18, 19.
- **qa — weak (20th week, unchanged):** `qa-verify` ran ~7.7×/day this week (46 events, all `ok`, all `reason: null`); still **0** PRs carry `kody:ui-verified` or `kody:ui-failed` (verified — 0 hits on both label queries), and the `kody:cto-decisions` trust-ledger label still does not exist. The same 9 open PRs are still waiting on a verdict (`#71`, `#109`, `#144`, `#154`, `#155`, `#158`, `#160`, `#162`, `#164`); #166 (`fix: preserve preview query when joining paths`) was merged 2026-06-10 by the operator with no `kody:ui-verified` label, bypassing the qa-verify gate. The docs-coverage PRs that `docs-code` cannot dispatch (see tech-writer) and the operator-merged preview fixes all bypass qa-verify, so the signal "is this safe to merge?" is still not being answered. **Effect:** verdict→merge pipeline remains open-loop for a 20th week.
- **clear-empty-goals (unowned, 20th week, unchanged):** still 0 entries in the activity log across 6 sampled days (2026-06-04 → 2026-06-10; 2026-06-06 missing as in weeks 17–19); the on-disk report at `.kody/reports/clear-empty-goals.md` is still the 2026-06-07 09:53:00Z finding (now 4 days stale, 4 days since the last duty-driven refresh); the untracked `.kody/goals/{kody-state-split,ai-company-orchestration-7-gap-plan}/state.json` files are still staged/added in the worktree (same as weeks 14–19). **Effect:** the manifest is being mutated faster than it can be scanned; the unowned duty has now been effectively stopped for six consecutive weeks. The `duty enable toggle` shipped 2026-06-09/10 is the operator's lever to formally park this.

**Changes since last week:** ceo steady→steady; coo idle→idle; cto unclear→unclear; kody idle→idle; qa weak→weak; tech-writer steady→steady; ux-designer idle→idle; *(unowned)* weak→weak. No grade changes this week. The structural state-persistence failure is now five ticks deep, and the new `duty enable toggle` series in `main` is the first operator-driven remediation in three weeks; the toggles are visible in the dashboard but the scheduler is not yet honoring them (2026-06-10 logged 35 events, indistinguishable from week 19's cadence).

**Activity log (week 20, 229 events across 6 days):** all 5 active duties ran on schedule (~7.7×/day each), all `outcome: completed`, all `outcomeKind: ok`, all `reason: null`. Tally per duty (2026-06-04 → 2026-06-10, sampled from `kody-state:.kody/activity/<date>.jsonl`): `ceo-performance-review` = 46, `dev-ci-health` = 46, `docs-code` = 46, `docs-readme` = 45, `qa-verify` = 46. `clear-empty-goals` = 0 across the same window. Per-day totals: 2026-06-04 = 50, 2026-06-05 = 5, 2026-06-07 = 54, 2026-06-08 = 40, 2026-06-09 = 45, 2026-06-10 = 35. (2026-06-06 missing from the log entirely — engine gap, not duty gap, same as weeks 17, 18, 19.) `docs-readme` missed a single slot (45 vs 46) for the fourth week running — within tolerance, no signal there. Total events this week (229) are within 2% of week 19 (224); the over-fire pattern is unmitigated.

**State machine (week 20, fifth tick):** input to this tick was `{"version":1,"rev":0,"cursor":"seed","data":{}}` — the same empty seed state as weeks 16, 17, 18, and 19. The engine has now had **five** consecutive opportunities to persist `ceo-performance-review.state.json` to the kody-state branch and has not done so. `data.cycle: 1` (week 16), `data.cycle: 2` (week 17), `data.cycle: 3` (week 18), `data.cycle: 4` (week 19), and `data.cycle: 5` (this week) all live only in this report body, not on disk. Until the engine writes the file, the dashboard "next run" readout remains decorative and the over-fire (~7.7×/day when cadence is 7d) is structurally unfixable from the duty side. Of the 5 active duties, only `dev-ci-health` has a state file on the kody-state branch — and it has not been touched in 4 days (~60 runs since the last write, vs ~50 in week 19). The operator's `Store reports on kody state branch` commit (2026-06-10T04:31:59Z) and the `duty enable toggle` series (2026-06-09T17:21Z → 2026-06-10T04:23Z) are the structural remediation in flight; if the scheduler is wired to honor the toggles, the cadence mismatch is closable from the operator side without further duty changes.

**Operator-driven work in the window (week 20):** substantive operator activity continued this week. Notable main-branch commits: `2026-06-04 #26: [Files] Fully-featured file browser and editor at /files (#78)`, `2026-06-05 chore(duties): migrate 25 legacy duties to folder-duties`, `2026-06-05 fix(duties): clear-empty-goals folder-duty runs target-less, writes a report`, `2026-06-08 feat(duty): add test-ex`, `2026-06-08 fix(chat): keep session context while switching`, `2026-06-08 Harden dashboard auth and scenario routes`, `2026-06-08 fix: control builders and duty executables`, `2026-06-08 chore: add goal 98 orchestration contracts (#156)`, `2026-06-09 feat: add duty enabled toggle` + `feat: add duty enable toggle` + `feat: improve files page interactions` + `fix: restore kody init workflow install` + `Fix Fly terminal bridge tunnel`, `2026-06-10 Refine duty control buttons` + `Store reports on kody state branch` + `feat(context): add ideas` + `Add memory manager and docs navigation` + `Add docs management and preview updates` + `fix: preserve preview query when joining paths (#166)` + `fix: preserve preview view query params` + `docs: require next-step question` (then `Revert`) + `Remove redundant Fly runner services` + `Restore Fly warm pool without shared LiteLLM` + `chore: upgrade to Next 16`. New this week vs week 19: the `duty enable toggle` series, `Store reports on kody state branch`, `feat(context): add ideas`, `Add memory manager and docs navigation`, `Add docs management and preview updates`, `feat(duty): add test-ex`, `fix: control builders and duty executables`, `Harden dashboard auth and scenario routes`, `fix(chat): keep session context while switching`, `chore: upgrade to Next 16`, and the Files browser (#78). None of these are addressed by any active duty (no `qa-verify` verdict on the preview fixes or on #78, no `dev-ci-health` label, no `docs-readme`/`docs-code` scan of the docs-management additions or the new memory manager, no `ceo` reconciliation of the "memory manager" or "test-ex" against the staff inventory). The `feat: add duty enable toggle` series is still the most consequential new capability in the window — the operator's structural answer to the over-fire pattern flagged in every weekly report since week 16.