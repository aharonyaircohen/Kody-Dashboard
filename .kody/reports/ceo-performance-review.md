# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

Twelfth week, picture unchanged from week 11: of 5 active duties, only ceo and tech-writer produced observable output; cto/qa/clear-empty-goals are now in their twelfth consecutive week of the same structural blockers (missing labels, missing owner, no state). Operator-driven work continued in the window — five more new operator-driven issues (#157, #159, #161, #163, #165) all about docs-coverage or the new engine duty contract, and the two goal state files surfaced in this worktree's git status as staged (`A` — added but not yet committed).

| Staff        | Owned duties  | Delivery | Consistency | Signal  | Grade   |
| ------------ | ------------- | -------- | ----------- | ------- | ------- |
| ceo          | 2 (1 active)  | High     | High        | High    | strong  |
| coo          | 3 (0 active)  | —        | —           | —       | idle    |
| cto          | 4 (1 active)  | Unclear  | Unclear     | Unclear | unclear |
| kody         | 11 (0 active) | —        | —           | —       | idle    |
| qa           | 3 (1 active)  | Low      | Low         | Low     | weak    |
| tech-writer  | 2 (2 active)  | Med      | Med         | Med     | steady  |
| ux-designer  | 1 (0 active)  | —        | —           | —       | idle    |

- **cto — unclear (twelfth week, unchanged):** dev-ci-health is the only active duty (15m cadence, would expect ~672 runs/week). The `kody:dev-ci-red` label still does not exist in the repo (24 `kody:` labels checked this tick — same set as last week; no churn). No `.kody/duties/dev-ci-health.state.json` has ever been committed; no `.kody/reports/dev-ci-health.md` has ever been committed. The duty body still claims the dev-ci-scheduler wrapper is the precondition, but no duty owns creating the label. **Effect:** the duty cannot deliver without an operator creating the label; the structural finding is now in its 12th consecutive week.

- **qa — weak (twelfth week, unchanged):** qa-verify is active (30m cadence, ~336 runs/week expected). The `kody:cto-decisions` trust-ledger label still does not exist in the repo; the `kody:ui-verified` / `kody:ui-failed` labels **do** exist (added by the operator earlier) but a fresh sweep of every PR/issue labeled with either returns zero stamps. No `.kody/duties/qa-verify.state.json` has ever been committed; no `.kody/reports/qa-verify.md` has ever been committed. **Effect:** the verdict→merge pipeline is open-loop for the 12th week; every recent delivery PR bypasses QA; the auto-merge branch is structurally dead until `kody:cto-decisions` is created.

- **tech-writer — steady (twelfth week, mostly unchanged):** both halves remain in their post-setup-check posture from last week — docs-code at 2026-06-09 08:40:30Z (`.kody/reports/docs-code.md` committed: coverage heuristic passes, 15/15 candidate folders at or near full coverage; the `useMediaQuery.ts` 1-line gap is the only sub-100% folder, sub-threshold for a tracking issue), docs-readme at 2026-06-09 04:56:53Z (`.kody/reports/docs-readme.md` committed: area→doc map in sync with the live `docs/` tree). Both surface the same engine-verb blocker: `chore --issue` is not in the engine README (verbs present in the freshly-checked engine list: `run`, `resolve`, `sync`, `merge`, `revert`, `release`, `release-prepare`, `release-publish`, `release-deploy`, `preview-build`, `duty-tick`, `duty-tick-scripted`, `duty-scheduler`, `goal-scheduler`, `goal-tick`, `init`, `worker-ask`, `chat`, `serve`, `brain-serve`, `pool-serve`, `runner-serve`, `stats`, `ci`). The operator's only unblocker is either (a) add a `chore` executable, or (b) substitute `run --issue` in the recommendation template. The duties themselves remain structurally a no-op — the setup checks document the state, they do not open drift issues or recommend PRs. **Effect:** same `steady` grade because the actionable work (recommend doc PRs) is still blocked on the same engine verb. The four new operator-driven doc-coverage issues opened in this window (#159 runners, #161 notifications/channels, #163 runners dup, #165 previews) are exactly the kind of PRs the duty would have recommended if the verb existed.

- **clear-empty-goals (unowned, twelfth week, drifting wider):** active every 1d, frontmatter still has no `staff:` field. Last meaningful report content generated 2026-06-07 09:53:00Z — commit at 2026-06-08 14:34:46Z was a byte-identical re-commit (churn, not delivery). The report on disk still scans 1 active goal (`kody-state-split`, 5 open tasks #50–#54), closed 0; the untracked `.kody/goals/ai-company-orchestration-7-gap-plan/state.json` (active since 2026-06-07 19:02Z) is now visible in this worktree's git status as `A` (staged/added) — first signal that the operator is preparing to commit it — but still not on the default branch. The `goal:ai-company-orchestration-7-gap-plan` label has 5 open issues (#92, #93, #94, #96, #97) and 3 closed (#90, #91, #95); none of the closed ones closed in this window. **Effect:** the process gap from the prior 11 weeks is still open; the 7-gap-plan state file's first appearance on disk is a small positive signal but not a delivery. Likely owner is still coo given their planning/audit posture, but assigning it is the operator call.

**Changes since last week:** none on grades — ceo strong→strong, coo idle→idle, cto unclear→unclear, kody idle→idle, qa weak→weak, tech-writer steady→steady, ux-designer idle→idle. The structural gaps in cto/qa and the unowned clear-empty-goals are now in their 12th consecutive week. The only net-new shape in the window was operator-driven, not duty-driven: four new operator commits on 2026-06-09 (`feat: improve files page interactions` `1f0030ce`, `Fix Fly terminal bridge tunnel` `de62042a`, `fix: restore kody init workflow install` `12086f54`, `feat: add duty enable toggle` `b01cb77b`); five new operator-driven issues opened 2026-06-08 → 2026-06-09 (#157 "Update dashboard for new engine duty contract", #159 / #161 / #163 / #165 all docs-coverage gaps in `runners/`, `notifications/channels/`, `runners/`, `previews/`); and the two goal state files staged for commit. The two tech-writer setup-check reports from last week remain the only duty-driven output in the window and are still first-tick setup artifacts, not drift work.

**Structural note (cross-cutting, unchanged from the last eleven weeks):** of the 5 active duties, 2 depend on labels that still do not exist in the repo (`kody:dev-ci-red` for cto/dev-ci-health, `kody:cto-decisions` for qa/qa-verify auto-merge branch). The other 3 (tech-writer two halves + clear-empty-goals) depend on either an engine verb that does not exist (`chore --issue`) or an owner who does not exist (clear-empty-goals has no `staff:` field). When a duty's own `gh label create` / `engine verb add` / `staff: <slug>` step does not run — or runs in a path that silently swallows its own error — the duty becomes structurally unable to deliver without surfacing a visible failure. The coo-owned duty-review and system-audit would catch this pattern at the design level if/when they are enabled.

**State-observability note (cross-cutting, unchanged from the last eleven weeks):** of the 5 active duties, **none** of cto/dev-ci-health, qa/qa-verify, tech-writer/docs-code, or tech-writer/docs-readme has a `.kody/duties/<slug>.state.json` ever committed to the repo. The ceo-performance-review itself is also still missing state between ticks (the seed state was unchanged at start of this tick). The tech-writer setup checks *did* set `data.lastRunISO` / `data.lastCheckedMergedAt` in their first tick — but the state file path is the engine's, and none of those four duties' state files are on disk to confirm. **Effect:** every "is this duty actually running?" question is still answered by inference (label drift, PR stamps, opened issues, report files) rather than by direct evidence.

**Self-finding (ceo-performance-review, twelfth week, unchanged):** the prior state shape (`{ cursor: "seed", data: {} }`) — confirmed unchanged at start of this tick — means the closing block is not being persisted between ticks. The report file has been committed 70 times in the past 7 days (latest at 22:52:50Z), well above the `every: 7d` cadence — the engine re-fires on every wake because `lastRunISO` is never written. **Effect:** next run readout cannot show a real cadence; cycle counter never advances through the state path; deltas in this report are hand-computed from the prior report body, not from `data.lastGrades`. This is now week 12 of the same finding.