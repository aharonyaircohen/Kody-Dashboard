Doc-coverage gap on `src/dashboard/lib/previews/` (issue #118) closed. All 14 modules now carry an `@ai-summary` JSDoc tag on their existing `@fileType` / `@domain` / `@pattern` header, and the central `fly-previews.ts` also grew a folder-level header (what the folder is, the reading-order entry points, and the three load-bearing gotchas — per-repo Fly billing, deterministic app naming, no dashboard-stored preview state). Summaries follow the convention used elsewhere in `src/dashboard/lib/`: prose that names the *why* and the *trap*, never restating what the code says. No code changes — comments only — so verify passes with no regressions.
