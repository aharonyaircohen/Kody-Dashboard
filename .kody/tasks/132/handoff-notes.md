Added a one-shot dev:brain pnpm script that uses concurrently to run the local Brain server (port 8080) and the Next.js dashboard together, prefixed output, one Ctrl+C to stop both. Added concurrently@^9.1.0 to devDependencies and refreshed the lockfile via `pnpm install --no-frozen-lockfile`. Existing `dev` and `brain:local` scripts are untouched. Verify (typecheck/lint/tests) passed on the first attempt. The two env keys (BRAIN_API_KEY for the server, BRAIN_CHAT_API_KEY for the dashboard) are both set to the same `local-dev-key` literal so the brain route's auth check accepts the locally-served agent.
