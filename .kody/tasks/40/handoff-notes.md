Resolved the six merge conflicts from `git merge origin/main` into PR #40.

**Strategy:** For every conflicted file, took the `origin/main` side (`--theirs`). The PR's branch (#40, "Files" feature) is several weeks behind main, so most conflicts reflect main's evolution rather than a clash of intents. The PR's own commits on the conflicted files are limited to a Prettier reformat and a small upload base64 fix (commit 8b08e494), neither of which are affected by the resolutions.

**Per-file notes:**

- **`.kody/context/todo.md`** — auto-regenerated by `clear-empty-goals` / `ceo-performance-review` duties. Kept main's version (most recent content); restoring the file rather than deleting it preserves the active context.
- **`.kody/reports/ceo-performance-review.md`** — auto-regenerated report. Kept main's version (newer "third consecutive week" cadence note).
- **`.kody/secrets.enc`** — AES-256-GCM-encrypted secrets blob. Always take main's version; the live main copy is the source of truth and can't be reconstructed.
- **`DashboardHome.tsx`** — main's `9986e2c0` refactored `DutiesHealth` into `GoalsOverview` / `ChannelsOverview` under a new `TriageStrip` pattern. Took main's full file. The old `DutiesHealth` was unreferenced and would have been dead code.
- **`ExecutablesManager.tsx`** — main's `92a142e5` unified executables onto `MasterDetailShell` (with new `ExecutableRow` + `ExecutableDetail` components). Took main's full file; the PR's inline `Card` list was replaced by the shell.
- **`executables/files.ts`** — main's `9986e2c0` removed the legacy-executables + markdown-duties code (`LEGACY_EXECUTABLES_DIR`, `FOLDER_DIRS`, `listFolderDutiesInDir`, `listMarkdownDutySummaries`, the `legacy` / `markdown` fields on `ExecutableSummary`). Took main's full file; those features were already removed in main, and the `Files` PR doesn't depend on them. `git grep` confirms no callers of the removed code in `src/`.

**Verification:** `pnpm format:check` clean. `pnpm lint` — 0 errors (124 pre-existing warnings, none on the resolved files except `ExecutablesManager.tsx` line 60-64 which are pre-existing unused-Dialog-subcomponent imports on main). `pnpm typecheck` — the only failures are pre-existing TS2307 errors for `@monaco-editor/react` on `FileDiffViewer.tsx` / `FileEditor.tsx` / `FileViewer.tsx` (the package is declared in `package.json` but not installed in `node_modules` in this environment); reproducing on `8b08e494` HEAD shows the same errors, so they are not caused by this merge. Logged in `followups.json`.
