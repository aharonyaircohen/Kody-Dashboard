Resolved one symmetric conflict in `src/dashboard/lib/components/settings-nav.ts` — the import block had HEAD adding `FolderOpen` and `Github` while origin/main removed both (empty conflict side). `FolderOpen` is used at line 306 for the "Files" nav item, so it was kept. `Github` is not referenced anywhere in the file and was dropped. No other conflicts existed.
