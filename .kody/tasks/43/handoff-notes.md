Added `@ai-summary` (with `@fileType`, `@domain`, `@pattern`) headers to the four channel adapters that lacked them: `slack.ts`, `discord.ts`, `generic.ts`, `telegram.ts`. Headers capture the key behavior and any gotcha (e.g., Discord's 2000-char cap + silent truncation, generic's client-side validator limitation). All 11/11 modules in `src/dashboard/lib/notifications/channels/` now carry the standard header. No behavioral changes. Pre-existing format warnings in 6 unrelated files — not touched.
